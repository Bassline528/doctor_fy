import 'package:flutter/material.dart';

class ConsultasScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Consulta Screen'),
      ),
    );
  }
}
